#include <suite.h>

t_suite			*suite_count_fails(t_suite *suite)
{
	// t_suite	*suite;

	// suite = (t_suite *)malloc(sizeof(t_suite));
	// suite->name = strdup(name);

	// suite->tests = lst_init();
	// suite->fn = fn;
	// return (suite);
	(void)suite;
	return (NULL);
}
